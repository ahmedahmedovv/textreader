[build]
  # No build command needed - this is a static site
  publish = "."
  functions = "netlify/functions"

[[redirects]]
  # Redirect all routes to index.html for SPA behavior
  # But exclude Netlify functions
  from = "/*"
  to = "/index.html"
  status = 200
  force = false

